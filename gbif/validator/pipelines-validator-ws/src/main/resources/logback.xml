<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="false">

    <!-- Appender de consola, sin async, para no perder líneas si el proceso termina de golpe -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <target>System.out</target>
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} %-5level [%thread] %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Nivel alto para ver bien el arranque de Spring/Spring Boot -->
    <logger name="org.springframework.boot" level="INFO"/>
    <logger name="org.springframework.boot.autoconfigure" level="INFO"/>
    <logger name="org.springframework.context" level="INFO"/>
    <logger name="org.springframework.web" level="INFO"/>

    <!-- Tu propio código, con más detalle mientras depuras -->
    <logger name="org.gbif.validator" level="DEBUG"/>

    <!-- Baja el ruido de librerías muy verbosas -->
    <logger name="org.apache.zookeeper" level="WARN"/>
    <logger name="org.apache.curator" level="WARN"/>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>

</configuration>
